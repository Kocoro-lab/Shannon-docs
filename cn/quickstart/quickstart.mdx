---
title: "快速入门"
description: "Submit your first task to Shannon in 5 minutes"
---

> 🚧 **翻译进行中** - 此文档正在从英文翻译为中文

**原文件**: `quickstart/quickstart.mdx`
**英文版本**: [查看英文文档](/en/quickstart/quickstart)

---


## 您的第一个任务

让我们向 Shannon 提交一个简单任务并看看它的运行。本指南假设您已完成[安装](/cn/quickstart/installation)。

## 使用 REST API

与 Shannon 交互的最简单方式是通过其 REST API：

```bash
curl -X POST http://localhost:8080/api/v1/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Analyze the sentiment of: Shannon makes AI agents simple and reliable!"
  }'
```

您将收到如下响应：

```json
{
  "task_id": "task-dev-1730000000",
  "status": "TASK_STATUS_RUNNING",
  "message": "Task submitted",
  "created_at": "2024-10-22T10:30:00Z"
}
```

## 检查任务状态

使用 `task_id` 检查状态：

```bash
curl http://localhost:8080/api/v1/tasks/task-dev-1730000000
```

响应：

```json
{
  "task_id": "task-dev-1730000000",
  "status": "TASK_STATUS_COMPLETED",
  "response": { "result": "The sentiment is overwhelmingly positive..." },
  "error": "",
  "query": "Analyze the sentiment of: Shannon makes AI agents simple and reliable!",
  "session_id": "...",
  "mode": "EXECUTION_MODE_STANDARD",
  "created_at": "2024-10-22T10:30:00Z",
  "updated_at": "2024-10-22T10:30:25Z"
}
```

## 实时流式传输事件

Shannon 支持 Server-Sent Events (SSE) 用于实时更新：

```bash
curl -N http://localhost:8080/api/v1/tasks/task-dev-1730000000/stream
```

您将看到事件实时发生：

```
event: AGENT_THINKING
data: {"message":"Analyzing sentiment..."}

event: TASK_COMPLETED
data: {"result":"The sentiment is positive..."}
```

## 使用 Python SDK

为了获得更好的开发体验，请使用 Python SDK：

### 安装

```bash
pip install shannon-sdk
```

### 提交和监控任务

```python
from shannon import ShannonClient

# gRPC for submission; SSE uses admin 8081 by default
client = ShannonClient()

handle = client.submit_task(
    query="Analyze the sentiment of: Shannon makes AI agents simple!"
)
print("Task submitted:", handle.task_id)

# Wait for completion
final = client.wait(handle.task_id, timeout=60)
print("Result:", final.result)
```

### 流式传输事件

```python
# Stream events in real-time
for event in client.stream(handle.workflow_id):
    print(f"{event.type}: {event.message}")
```

## 执行模式

Shannon 根据任务复杂度内部自动选择执行模式。REST API 目前不接受 `mode` 参数。

## 成本控制

预算控制由 Shannon 管理。通过 REST 显式设置每个请求的预算配置尚未可用。

## 多轮对话

Shannon 支持基于会话的对话：

```python
session_id = "my-conversation-123"

# First message
handle1 = client.submit_task(
    query="What is the capital of France?",
    session_id=session_id
)

# Follow-up message (Shannon remembers context)
handle2 = client.submit_task(
    query="What's the population?",
    session_id=session_id
)

# Shannon 知道您问的是巴黎的人口
```

## 使用控制面板

打开 http://localhost:2111 访问交互式控制面板：

<Steps>
  <Step title="提交任务">
    使用任务提交表单创建具有自定义配置的任务
  </Step>
  <Step title="监控执行">
    观察实时事件流，显示智能体思考、工具调用和进度
  </Step>
  <Step title="查看指标">
    跟踪所有任务的 token 使用、成本、延迟和成功率
  </Step>
  <Step title="调试工作流">
    点击任务查看详细的执行跟踪和 Temporal 工作流历史
  </Step>
</Steps>

## 示例用例

<Note>
Shannon 根据查询复杂度自动选择适当的执行模式和认知策略。您无需指定这些参数。
</Note>

### 情感分析

```python
client.submit_task(
    query="Analyze sentiment: The product exceeded my expectations!"
)
```

### 研究和摘要

```python
client.submit_task(
    query="Research quantum computing breakthroughs in 2024 and create a summary"
)
```

### 代码生成

```python
client.submit_task(
    query="Write a Python function to calculate Fibonacci numbers with memoization"
)
```

### 复杂分析（带上下文）

```python
client.submit_task(
    query="Compare the pros and cons of microservices vs monolithic architecture",
    context={
        "domain": "software_architecture",
        "depth": "comprehensive",
        "include_examples": True
    }
)
```

## 下一步是什么？

<CardGroup cols={2}>
  <Card
    title="核心概念"
    icon="book"
    href="/cn/quickstart/concepts/agents"
  >
    了解智能体、工作流和认知模式
  </Card>
  <Card
    title="API 参考"
    icon="code"
    href="/cn/api/overview"
  >
    探索所有 API 端点和参数
  </Card>
  <Card
    title="Python SDK 指南"
    icon="python"
    href="/cn/sdk/python/quickstart"
  >
    深入了解 SDK 功能和异步用法
  </Card>
  <Card
    title="配置"
    icon="gear"
    href="/cn/quickstart/configuration"
  >
    根据您的需求自定义 Shannon
  </Card>
</CardGroup>

## 故障排除

<AccordionGroup>
  <Accordion title="任务停留在 'running' 状态">
    检查日志中的错误：

    ```bash
    docker compose logs orchestrator
    docker compose logs agent-core
    ```

    常见原因：
    - 无效的 LLM API 密钥
    - LLM 提供商速率限制
    - 网络连接问题
  </Accordion>

  <Accordion title="身份验证错误">
    Shannon 默认禁用身份验证运行（`GATEWAY_SKIP_AUTH=1`）。如果您已启用身份验证，请包含 API 密钥：

    ```bash
    curl -H "X-API-Key: sk_test_123456" \
      http://localhost:8080/api/v1/tasks
    ```
  </Accordion>

  <Accordion title="高成本 / token 使用">
    预算限制通过环境变量在平台级别配置：

    ```bash
    # 在 .env 文件中
    MAX_TOKENS_PER_REQUEST=10000
    MAX_COST_PER_REQUEST=0.50

    # 重启服务以使更改生效
    docker compose restart
    ```

    对于每任务监控，通过状态端点或流式事件检查成本。
  </Accordion>
</AccordionGroup>


---

## 参与翻译

如果您想帮助翻译此文档，请访问我们的 [GitHub 仓库](https://github.com/Kocoro-lab/Shannon)。
